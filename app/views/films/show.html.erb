<p id="notice"><%= notice %></p>
  <b>Type:</b>
  <%= @film.type %>
  |  
  <b>Format:</b>
  <%= @film.format %>
  |  
  <b>Brand:</b>
  <%= @film.brand %>
  |  
  <b>Name:</b>
  <%= @film.name %>
  |  
  <b>Description:</b>
  <%= @film.description %>
</p>
<div>
	<% @film.photos.each do |photo| %>
	  <%= link_to image_tag(photo.image.url(:medium), :border =>0, :class => 'thumb'),:controller =>'photos', :action => 'show', :id => photo %>
	<% end %>
</div>
<%-if user_signed_in?-%>
<%= link_to 'Edit', edit_film_path(@film) %> |
<%-end-%>
<%= link_to 'Back', films_path %>
